services:
  glauth:
    image: glauth/glauth:latest
    environment:
      GLAUTH_CONFIG: /app/config/config.cfg
    volumes:
      - ./testldap/default-config.cfg:/app/config/config.cfg:ro
      - ./testldap/cert.pem:/app/config/cert.pem:ro
      - ./testldap/key.pem:/app/config/key.pem:ro
    ports:
      - "3890:389"

  authbroker:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      AUTHBROKER_CONFIG: /etc/authbroker/config.json
      AUTHBROKER_DATA: /tmp/authbroker-data/data.json
    depends_on:
      - glauth
    volumes:
      - ./compose/authbroker.config.json:/etc/authbroker/config.json:ro
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 20

  test-web-ui:
    build:
      context: ./test-web-ui
      dockerfile: Dockerfile
    environment:
      WEB_UI_LISTEN: ":8090"
      PUBLIC_BASE_URL: "http://localhost:8090"
      AUTHBROKER_PUBLIC_URL: "http://localhost:8080"
      AUTHBROKER_INTERNAL_URL: "http://authbroker:8080"
      OAUTH_CLIENT_ID: "test-web-ui"
      OAUTH_CLIENT_SECRET: "test-web-ui-secret"
    depends_on:
      authbroker:
        condition: service_healthy
    ports:
      - "8090:8090"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8090/healthz"]
      interval: 5s
      timeout: 3s
      retries: 20

  passkey-demo:
    build:
      context: ./passkey-demo
      dockerfile: Dockerfile
    environment:
      PASSKEY_DEMO_LISTEN: ":8091"
      PUBLIC_BASE_URL: "http://localhost:8091"
      AUTHBROKER_PUBLIC_URL: "http://localhost:8080"
      AUTHBROKER_INTERNAL_URL: "http://authbroker:8080"
    depends_on:
      authbroker:
        condition: service_healthy
    ports:
      - "8091:8091"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8091/healthz"]
      interval: 5s
      timeout: 3s
      retries: 20
